_base_: layout_L4_region_pareto_llm_mpvs_paper_cec_nollm_exp.yaml

# Paper baseline: short-term score-gated controller (no realized long-gain release).
# Keeps the same MPVS verifier + controller, but disables the realized-credit-first release rule.
detailed_place:
  mpvs:
    controller:
      cec:
        # Legacy rule: promote/release only when trial_score/edge_local is non-negative.
        release_rule: score_gate_only
        # Remove candidate bridge to make this a cleaner short-term baseline.
        candidate_release: false
        family_stage_prior: false
