_base_: layout_L4_region_pareto_llm_mpvs_bc2cec_nollm_exp.yaml

# Ablation: disable stage probes (keeps BC^2-CEC controller intact).
detailed_place:
  mpvs:
    macros:
      probe:
        enabled: false
