_base_: S015_A1_lowtax_B2_cfboost_signedaos_K4_bc2cec.yaml

# Pure probe-CF ablation:
# keep the repaired transmission path identical,
# only remove probe-side CF estimation itself.
detailed_place:
  mpvs:
    macros:
      probe:
        cf_discount: 0.0
        cf_reliability_tau: 0.0
        weight_metric: raw
        cf_one_sided_update: false
        # IMPORTANT: keep shrinkage ON so this remains a clean CF-only ablation
