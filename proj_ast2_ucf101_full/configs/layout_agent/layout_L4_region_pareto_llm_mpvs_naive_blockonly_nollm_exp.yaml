_base_: layout_L4_region_pareto_llm_mpvs_naive_mixed_nollm_exp.yaml

# Headroom probe: block-relocate macro only (no controller).
detailed_place:
  mpvs:
    proposer:
      quota:
        macro: 3
        mem: 0
    memory:
      enabled: false
    controller:
      enabled: false
    macros:
      # Always enable and ONLY use block-relocate macro
      base: ["block_relocate"]
      extra_on_stagnation: []
      enable_base_when_stagnation_ge: 0
      enable_extra_when_stagnation_ge: 999999
      max_macros: 1
      n_steps: 1
      block_relocate:
        # DANGEROUS block_relocate (match mpvs_exp.yaml)
        block_size: 6
        seed_topk: 24
        partner_topk: 16
        external_partner_topk: 10
        anchor_topk: 16
        candidate_blocks: 96
        eval_topk: 24
        use_empty_only: false
        perm_sites_k: 12
        perm_limit: 500
        score_w_est: 1.0
        score_w_internal: 1.0e-6
        score_w_compact: 1.0e-6
        score_w_centroid: 5.0e-7
