_base_: configs/vc_phase3_pruningonly_ucf101_A_hwloss_fast20_k92.yaml

# HWSURR baseline: enable straight-through surrogate gradients for keep_factors.
# Value still comes from discrete proxy; gradients come from surrogate.

hw:
  surrogate_grad:
    enabled: true
    weight: 1.0
