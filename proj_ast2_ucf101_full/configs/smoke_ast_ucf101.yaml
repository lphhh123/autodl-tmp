_base_: ast2_ucf101.yaml
data:
  cache_enabled: true
  cache_dir: outputs/cache/ucf101
train:
  epochs: 1
  out_dir: outputs/SMOKE/ast2_ucf101

val:
  fast_max_batches: 200
  full_every_epochs: 999999
  log_interval: 50
  use_tqdm: true

test:
  run_final_test: false
